$(document).ready(function () {
    $('.arianne__dot__1').switchstyler({ //fixed object
        on: '#section1', //target section
        addClass: 'highlight', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.arianne__dot__2').switchstyler({ //fixed object
        on: '#section3', //target section
        addClass: 'highlight', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.arianne__dot__3').switchstyler({ //fixed object
        on: '#section5', //target section
        addClass: 'highlight', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.arianne__dot__4').switchstyler({ //fixed object
        on: '#section7', //target section
        addClass: 'highlight', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.arianne__dot__5').switchstyler({ //fixed object
        on: '#section9', //target section
        addClass: 'highlight', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.mover').switchstyler({ //fixed object
        on: 'footer', //target section
        addClass: 'none', //classes to add e.g. 'class1 class4 class6'
    });
});

$(document).ready(function () {
    $('.petiteBarre').switchstyler({ //fixed object
        on: 'footer', //target section
        addClass: 'none', //classes to add e.g. 'class1 class4 class6'
    });
});
